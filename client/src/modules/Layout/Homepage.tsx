import { FC, ReactElement } from 'react';

const Homepage: FC = (): ReactElement => {
  return <div className="text-gray-300 bg-background-dark min-h-screen relative ">Homepage</div>;
};

export default Homepage;
